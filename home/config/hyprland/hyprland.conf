$fileManager=dolphin
$menu=wofi --show drun
$mod=SUPER
$terminal=alacritty

exec-once = hyprctl setcursor volantes-cursors 10

animations {
  enabled=no
}

decoration {
  blur {
    enabled=true
    passes=1
    size=3
    vibrancy=0.169600
  }

  shadow {
    color=rgba(1a1a1aee)
    enabled=true
    range=4
    render_power=3
  }
  active_opacity=0.950000
  inactive_opacity=0.850000
  rounding=0
}

dwindle {
  preserve_split=true
  pseudotile=true
}

general {
  allow_tearing=false
  border_size=2
  col.active_border=rgba(33ccffee) rgba(00ff99ee) 45deg
  col.inactive_border=rgba(595959aa)
  gaps_in=0
  gaps_out=0
  layout=dwindle
  resize_on_border=false
}

input {
  follow_mouse=1
  kb_layout=jp
  sensitivity=0

  tablet {
    absolute_region_position = true
  }
}

master {
  new_status=master
}

misc {
  vfr=false
}

bind=$mod, Return, exec, $terminal
bind=$mod, Q, killactive
bind=$mod, R, exec, $menu
bind=$mod, F, togglefloating
bind=$mod, E, exec, $fileManager
bind=$mod, P, pseudo
bind=$mod, J, togglesplit
bind=$mod, left, movefocus, l
bind=$mod, right, movefocus, r
bind=$mod, up, movefocus, u
bind=$mod, down, movefocus, d

bind=$mod, 1, workspace, 1
bind=$mod, 2, workspace, 2
bind=$mod, 3, workspace, 3
bind=$mod, 4, workspace, 4
bind=$mod, 5, workspace, 5
bind=$mod, 6, workspace, 6
bind=$mod, 7, workspace, 7
bind=$mod, 8, workspace, 8
bind=$mod, 9, workspace, 9
bind=$mod, 0, workspace, 10

bind=$mod SHIFT, 1, movetoworkspace, 1
bind=$mod SHIFT, 2, movetoworkspace, 2
bind=$mod SHIFT, 3, movetoworkspace, 3
bind=$mod SHIFT, 4, movetoworkspace, 4
bind=$mod SHIFT, 5, movetoworkspace, 5
bind=$mod SHIFT, 6, movetoworkspace, 6
bind=$mod SHIFT, 7, movetoworkspace, 7
bind=$mod SHIFT, 8, movetoworkspace, 8
bind=$mod SHIFT, 9, movetoworkspace, 9
bind=$mod SHIFT, 0, movetoworkspace, 10

bind=$mod, S, togglespecialworkspace, magic
bind=$mod SHIFT, S, movetoworkspace, special:magic

bindm=$mod, mouse:272, movewindow
bindm=$mod, mouse:273, resizewindow

# hyprshotでスクショを取る.
# 使用感はspectacleに近い感じ.
bind = , Print, exec, hyprshot --clipboard-only -z -m  region

# フルスクリーン
bind = $mod SHIFT, F, fullscreen 

# 透過のトグル
bind = $mod, O, exec, hyprctl dispatch toggleopaque
windowrule=nofocus,class:^$,title:^$,xwayland:1,floating:1,fullscreen:0,pinned:0
